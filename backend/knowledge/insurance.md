# Insurance & Payment

Aarogya accepts self-pay (cash, card, UPI) at both branches. For insurance-specific coverage
questions, or if a caller asks whether a particular insurer or TPA is accepted, log it as a follow-up
(`log_followup`, reason "insurance_query") so the billing desk can call back with an accurate answer —
do not state coverage details you're not certain of.
